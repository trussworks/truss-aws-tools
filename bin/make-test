#! /bin/sh

go_test_output="/tmp/go.test.$$"

go test github.com/trussworks/truss-aws-tools/... | tee "${go_test_output}"

if [[ ! -z "$JUNIT_OUTPUT_DIR" ]]; then
    bin/go-junit-formatter < "${go_test_output}" \
                           > "${JUNIT_OUTPUT_DIR}/gotest.xml"
fi
